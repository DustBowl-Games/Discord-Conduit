# Copy this file to `.env` and fill in your bot token.
#   cp .env.example .env
#
# The .env file is loaded automatically by `docker compose` (see docker-compose.yml)
# and is git-ignored — never commit a real token.

# Discord bot token. Required for the bot to authenticate with Discord.
DISCORD_CONDUIT_TOKEN=
